Vagrant.configure("2") do |config|

  config.vm.box = "ubuntu/xenial64"
  
  # Add a private network between the host and the VM using ip.
  config.vm.network "private_network", ip: "192.168.10.100"

end
